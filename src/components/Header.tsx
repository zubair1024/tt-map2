import React from "react";

const Header = () => {
  return (
    <div>
      <div className="navbar bg-base-100">
        <a className="btn btn-ghost text-xl">TT Analyzer</a>
      </div>
    </div>
  );
};

export default Header;
